{
  "name": "snippex",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "frontend/**/*.{ts,tsx}": [
      "npm run lint --prefix frontend"
    ],
    "backend/**/*.ts": [
      "npm run lint --prefix backend"
    ]
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0"
  }
}
